// Apply Button Object Method
// applies the current style to the form definition

// closes the "TextStyle" dialog window
Form.applyButtonClicked:=True
ACCEPT

// the form data is sent back to the "openTextStyleDialog" class 
// which will apply the current style if the "Apply to selection" is selected
